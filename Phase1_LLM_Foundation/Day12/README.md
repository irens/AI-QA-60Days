# Day 12: 输出鲁棒性测试——跨语言与语篇级改写

## 🎯 1. 核心风险与测试目标

### 1.1 测试工程师视角
> **核心原则**：开发只管跑通，我们要想办法把它搞崩溃。跨语言场景下，翻译往返可能丢失关键信息；语篇级改写可能破坏长距离依赖。这些"高级"改写是鲁棒性测试的深水区。

### 1.2 业务风险点

| 风险类别 | 具体风险 | 线上事故场景 |
|---------|---------|-------------|
| **跨语言语义漂移** | 翻译往返后核心语义改变 | 多语言客服中，中文→英文→中文后答案错误 |
| **文化概念丢失** | 文化特定概念在翻译中消失 | "春节"翻译成"Spring Festival"后失去文化内涵 |
| **语篇连贯性破坏** | 段落重组后逻辑关系混乱 | 长文档摘要重组后因果关系颠倒 |
| **长距离依赖失效** | 跨段落指代关系丢失 | 多轮对话中，代词指代错误 |
| **细节信息遗漏** | 详略调整后关键细节丢失 | 简化表述后，重要约束条件被省略 |

### 1.3 测试思路

**跨语言鲁棒性测试**：
- **翻译往返测试**：中文→英文→中文，验证语义一致性
- **多语言平行测试**：同一问题用不同语言提问，对比答案
- **文化概念保留测试**：验证文化特定概念的传递

**语篇级改写测试**：
- **段落重组**：调整段落顺序，验证逻辑一致性
- **详略调整**：扩展或压缩内容，验证关键信息保留
- **衔接词替换**：改变连接词，验证逻辑关系保持
- **指代消解**：测试跨句子指代关系的鲁棒性

---

## 📚 2. 跨语言与语篇鲁棒性原理

### 2.1 跨语言语义漂移问题

```
┌─────────────────────────────────────────────────────────────────┐
│                    跨语言语义漂移机制                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  问题本质：不同语言的概念映射并非一一对应                          │
│                                                                 │
│  示例1: 文化概念丢失                                              │
│  中文: "春节是中国最重要的传统节日"                               │
│  ↓ 翻译                                                           │
│  英文: "Spring Festival is the most important traditional        │
│         festival in China"                                       │
│  ↓ 回译                                                           │
│  中文: "春节是中国最重要的传统节日" ✅ 保留                       │
│                                                                 │
│  示例2: 细微语义差异                                              │
│  中文: "他有点生气"                                               │
│  ↓ 翻译                                                           │
│  英文: "He is a little angry" / "He is kind of angry"            │
│  ↓ 回译                                                           │
│  中文: "他有点生气" / "他挺生气的" ⚠️ 程度差异                   │
│                                                                 │
│  示例3: 概念缺失（严重）                                          │
│  中文: "张三打了李四一巴掌"                                       │
│  ↓ 翻译                                                           │
│  英文: "Zhang San slapped Li Si"                                 │
│  ↓ 回译                                                           │
│  中文: "张三扇了李四一耳光" ✅ 基本保留                          │
│                                                                 │
│  但如果模型不理解"slap"的精确含义...                              │
│  可能回译为: "张三拍了李四一下" ❌ 严重程度降低                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 语篇级改写风险

```
┌─────────────────────────────────────────────────────────────────┐
│                    语篇级改写风险分析                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  风险1: 长距离依赖破坏                                            │
│                                                                 │
│  原文:                                                            │
│    [段落1] 公司A在2020年推出了产品X。                            │
│    [段落2] 这款产品大获成功...                                    │
│    [段落3] 然而，竞争对手公司B在2021年推出了类似产品...           │
│                                                                 │
│  重组后:                                                          │
│    [段落3] 竞争对手公司B在2021年推出了类似产品...                 │
│    [段落1] 公司A在2020年推出了产品X。                            │
│    [段落2] 这款产品大获成功...                                    │
│                                                                 │
│  问题: "这款产品"指代不明，可能误解为公司B的产品                  │
│                                                                 │
│  ─────────────────────────────────────────────────────────────  │
│                                                                 │
│  风险2: 逻辑关系丢失                                              │
│                                                                 │
│  原文: "因为下雨了，所以地面湿了"                                 │
│  改写: "地面湿了。下雨了。"                                       │
│                                                                 │
│  问题: 因果关系不明确，可能误解为:                                │
│        "地面湿了，所以下雨了"（因果颠倒）                         │
│                                                                 │
│  ─────────────────────────────────────────────────────────────  │
│                                                                 │
│  风险3: 关键细节省略                                              │
│                                                                 │
│  原文: "请在24小时内完成支付，否则订单将自动取消"                 │
│  简化: "请尽快完成支付"                                           │
│                                                                 │
│  问题: 时间约束（24小时）和后果（自动取消）丢失                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.3 测试方法详解

```python
# 跨语言鲁棒性测试核心逻辑

class CrossLingualTester:
    """跨语言鲁棒性测试器"""
    
    def test_translation_roundtrip(self, text: str, model) -> RobustnessResult:
        """
        翻译往返测试
        
        流程：
        1. 中文 → 英文
        2. 英文 → 中文
        3. 对比原文和回译文本的语义一致性
        """
        # Step 1: 翻译
        translated = self.translate(text, src="zh", tgt="en")
        print(f"翻译结果: {translated}")
        
        # Step 2: 回译
        back_translated = self.translate(translated, src="en", tgt="zh")
        print(f"回译结果: {back_translated}")
        
        # Step 3: 语义一致性计算
        consistency = self.semantic_similarity(text, back_translated)
        
        # Step 4: 关键信息保留检查
        key_info_retention = self.check_key_info_retention(text, back_translated)
        
        return RobustnessResult(
            test_type="translation_roundtrip",
            original=text,
            back_translated=back_translated,
            consistency=consistency,
            key_info_retention=key_info_retention,
            is_robust=consistency > 0.85 and key_info_retention > 0.9
        )
    
    def test_multilingual_parallel(self, question: str, model) -> RobustnessResult:
        """
        多语言平行测试
        
        用不同语言问同一问题，对比答案一致性
        """
        languages = ["zh", "en", "ja", "de"]
        translations = {
            lang: self.translate(question, src="zh", tgt=lang)
            for lang in languages
        }
        
        # 获取各语言答案
        answers = {
            lang: model.generate(translations[lang])
            for lang in languages
        }
        
        # 翻译回中文进行对比
        answers_zh = {
            lang: self.translate(ans, src=lang, tgt="zh")
            for lang, ans in answers.items()
        }
        
        # 计算答案一致性
        consistency_matrix = self.compute_consistency_matrix(answers_zh)
        
        return RobustnessResult(
            test_type="multilingual_parallel",
            answers=answers_zh,
            consistency_matrix=consistency_matrix,
            is_robust=all(c > 0.8 for c in consistency_matrix.values())
        )


class DiscourseTester:
    """语篇级改写测试器"""
    
    def test_paragraph_reordering(self, text: str, model) -> RobustnessResult:
        """
        段落重组测试
        """
        paragraphs = text.split("\n\n")
        
        # 生成重组变体
        reordered = paragraphs[::-1]  # 简单反转
        reordered_text = "\n\n".join(reordered)
        
        # 获取摘要或回答
        original_output = model.summarize(text)
        reordered_output = model.summarize(reordered_text)
        
        # 检查一致性
        consistency = self.semantic_similarity(original_output, reordered_output)
        
        # 检查关键事实保留
        key_facts_original = self.extract_key_facts(original_output)
        key_facts_reordered = self.extract_key_facts(reordered_output)
        fact_retention = len(key_facts_original & key_facts_reordered) / len(key_facts_original)
        
        return RobustnessResult(
            test_type="paragraph_reordering",
            consistency=consistency,
            fact_retention=fact_retention,
            is_robust=consistency > 0.8 and fact_retention > 0.9
        )
    
    def test_coreference_resolution(self, text: str, model) -> RobustnessResult:
        """
        指代消解鲁棒性测试
        """
        # 测试代词指代
        text_with_pronoun = "张三和李四去公园。他在那里遇到了王五。"
        
        # 询问"他"指谁
        question = "他指的是谁？"
        answer = model.generate(text_with_pronoun + question)
        
        # 验证答案
        is_correct = "张三" in answer or "不确定" in answer
        
        return RobustnessResult(
            test_type="coreference_resolution",
            answer=answer,
            is_correct=is_correct,
            is_robust=is_correct
        )
```

### 2.4 评估指标

| 指标名称 | 计算方法 | 风险阈值 | 业务含义 |
|---------|---------|---------|---------|
| **翻译往返一致性** | 原文与回译文本的语义相似度 | < 0.85 | 跨语言语义漂移 |
| **关键信息保留率** | 保留的关键信息数 / 原文关键信息数 | < 0.9 | 重要信息丢失 |
| **多语言答案一致性** | 不同语言答案的语义相似度 | < 0.8 | 多语言服务不一致 |
| **段落重组稳定性** | 重组前后输出的一致性 | < 0.8 | 语序依赖严重 |
| **指代消解准确率** | 正确指代次数 / 总指代次数 | < 0.95 | 长距离依赖脆弱 |

---

## 🧪 3. 实验验证任务

请运行本目录下的 `test_day12.py`，观察以下关键输出：

### 3.1 跨语言鲁棒性测试
- 翻译往返一致性
- 多语言平行测试
- 文化概念保留验证

### 3.2 语篇级改写测试
- 段落重组稳定性
- 详略调整测试
- 指代消解鲁棒性

### 3.3 综合评估
- 跨维度鲁棒性评分
- 高风险场景识别
- 改进建议生成

---

## 📝 4. 产出要求

将运行结果贴回给 Trae，让其生成 `report_day12.md` 质量分析报告。

报告应包含：
1. **跨语言测试结果**：翻译往返一致性、多语言答案一致性
2. **语篇级改写结果**：段落重组稳定性、指代消解准确率
3. **风险场景清单**：高风险的跨语言和语篇改写模式
4. **改进建议**：提升跨语言和语篇鲁棒性的工程实践
